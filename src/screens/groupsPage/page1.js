import React from "React";
import { View, StyleSheet } from "react-native";

export default class Page extends React.Component {
    render() {
        return (
            <View style={{ backgroundColor: '#0000' }}></View>
        )
    }
}